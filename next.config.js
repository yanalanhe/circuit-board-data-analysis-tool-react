/** @type {import('next').NextConfig} */
const nextConfig = {
  // React Strict Mode for development warnings
  reactStrictMode: true,
  // App Router handles CSR by default in Next.js 14
  // No SSR configuration needed for MVP
}

module.exports = nextConfig
